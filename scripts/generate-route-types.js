import fs from 'node:fs'
import path from 'node:path'

const outputFile = path.resolve(process.cwd(), 'src/types/route-types.gen.d.ts')

const content = `// Auto-generated by scripts/generate-route-types.js
export type AppRouteName =
  | 'home'
  | 'learning-hub'
  | 'vocabulary'
  | 'exam'
  | 'games-hub'
  | 'game-reaction'
  | 'game-number-guess'
  | 'game-memory'
  | 'game-pop-blast'
`

fs.mkdirSync(path.dirname(outputFile), { recursive: true })
fs.writeFileSync(outputFile, content)
